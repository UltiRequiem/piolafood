/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["piolafood.wants.solutions", "piolafood.is-from.space"],
	},
};

module.exports = nextConfig;
